<article class="h-entry h-entry--reply">
  {% set target = post.data["in-reply-to"] | first %}
  <header>
      <small>
          <strong>↩ Replied to </strong><a class="u-in-reply-to" href="{{ target }}">{{ target }}</a>
      </small>
  </header>

  <p class="e-content p-name">
    {{ post.templateContent | safe }}
  </p>
</article>
